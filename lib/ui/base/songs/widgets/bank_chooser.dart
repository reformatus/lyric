import 'package:flutter/material.dart';

class BankChooser extends StatelessWidget {
  const BankChooser({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Bank választó!!!'));
  }
}
