import '../../data/bank/bank.dart';

Future setBankEnabled(bool enabled, Bank bank) {
  throw UnimplementedError();
}

Future setBankOfflineMode(bool offlineMode, Bank bank) {
  throw UnimplementedError();
}
